services:
    # FastAPI
  fastapi:
    build:
      context: ..
      dockerfile: docker/mentra_ai/Dockerfile
    container_name: fastapi
    ports:
      - "8000:8000"
    volumes:
      - fastapi_data:/app/assets
    networks:
      - backend
    env_file:
      - ./env/.env.app
    depends_on:
      redis:
        condition: service_healthy

  # Redis Stack — includes RediSearch (FT.*), RedisJSON, RedisBloom
  # Latest pinned release: 7.4.0-v8 (confirmed from hub.docker.com/r/redis/redis-stack-server/tags)
  redis:
    image: redis/redis-stack-server:7.4.0-v8
    container_name: redis_cache
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    # IMPORTANT: Never override `command:` on this image — doing so runs plain redis-server
    # which bypasses the Stack entrypoint and skips loading all modules (FT.*, JSON, Bloom).
    # Password and persistence flags are set via REDIS_ARGS in ./env/.env.redis instead.
    env_file:
      - ./env/.env.redis
    networks:
      - backend
    healthcheck:
      test: ["CMD-SHELL", "redis-cli ping | grep -E -q 'PONG|NOAUTH'"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  # Nginx Service 
  nginx:
    image: nginx:stable-alpine3.23-perl
    container_name: nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - fastapi
    networks:
      - backend
    restart: always


  # # Prometheus Monitoring
  # prometheus:
  #   image: prom/prometheus:v3.10.0
  #   container_name: prometheus
  #   ports:
  #     - "9090:9090"
  #   volumes:
  #     - prometheus_data:/prometheus
  #     - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
  #   networks:
  #     - backend
  #   restart: always

  # # Grafana Dashboard
  # grafana:
  #   image: grafana/grafana:12.4.0-ubuntu
  #   container_name: grafana
  #   ports:
  #     - "3000:3000"
  #   volumes:
  #     - grafana_data:/var/lib/grafana
  #   env_file:
  #     - ./env/.env.grafana
  #   depends_on:
  #     - prometheus
  #   networks:
  #     - backend
  #   restart: always

  # # Node Exporter for system Metrics
  # node-exporter:
  #   image: prom/node-exporter:v1.10.2
  #   container_name: node-exporter
  #   ports:
  #     - "9100:9100"
  #   networks:
  #     - backend
  #   volumes:
  #     - /proc:/host/proc:ro
  #     - /sys:/host/sys:ro
  #     - /:/rootfs:ro
  #   command:
  #     - '--path.procfs=/host/proc'
  #     - '--path.sysfs=/host/sys'
  #     - '--path.rootfs=/rootfs'
  #     - '--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)'
  #   restart: always
  
networks:
  backend:
    driver: bridge

volumes:
  fastapi_data:
  redis_data:
  # prometheus_data:
  # grafana_data:
